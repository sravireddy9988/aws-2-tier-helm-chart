apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.backend.servicename }}
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  clusterIP: None  # Headless or remove this line to make it a standard ClusterIP